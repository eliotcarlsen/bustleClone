<h1>Bustle</h1>
  {{#each model.stories as |story|}}
    {{story-tile story=story}}
  {{/each}}
  <h2>All Comments</h2>
  {{#each model.comments as |comment|}}
    <li>{{comment.user}}:</li>
    "{{comment.comment}}"
  {{/each}}
